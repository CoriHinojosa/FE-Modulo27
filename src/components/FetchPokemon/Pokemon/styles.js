import styled from 'styled-components';

const PokeUl = styled.ul`
    padding: 3px;
    list-style-type:none;
    display:flex;
    flex-direction: column; 
    align-items: center;
`;

const PokeLi = styled.li`
    font-size: 1.2rem;
`;

export { PokeUl, PokeLi };
